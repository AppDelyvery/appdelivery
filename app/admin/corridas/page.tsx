import EmConstrucao from "@/components/admin/EmConstrucao";

export default function Page() {
  return <EmConstrucao title="Corridas" msg="Lista de todas as corridas + detalhe com comprovante — próxima rodada (seção 2)." />;
}
